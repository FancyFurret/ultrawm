[package]
name = "application-services"
version = "0.1.0"
edition = "2021"

[build-dependencies]
bindgen = "0.65.1"

# TODO: Can we just make this whole crate only compile for macos?
[target.'cfg(target_os = "macos")'.dependencies]
core-foundation = "0.9.3"
core-graphics = "0.23.1"
